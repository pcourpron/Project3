var equal = require('deep-strict-equal');
const router = require("express").Router();


router.post('/checkAnswer',function(req,response){
    let tests = req.body.test
    test.forEach(element =>{


})
})